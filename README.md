The goal is internalize the fundamentals of core mechanics of Unity Engine. 

Creating a Player Controller in Unity

Scene transition with DontDestroyOnLoad method

MusicManager(Singleton Pattern), CameraManager(raw camera using, without cinemachine)
